@inject AudioManager AudioManager

<ScollableModalView IsActive="isActive"
    OnCloseButtonClick="OnClose">
    <div style="margin-top:60px;padding:10px">
        @if (currentPalylistId != null)
        {
            <button value="submit" class="btn" style="width:150px;color:white;background-color:orange;margin-top:10px"
                    @onclick="AddToPlaylist">
                Add To Playlist
            </button>
        }
        
        <UserPlaylistView IsInAddMode="true" OnPlaylistSelected="PlaylistSelected" />
    </div>
</ScollableModalView>

<style>
    .btn {
        margin: 0 12px;
        padding: 10px 0;
        border: none;
        border-radius: 25px;
        font-weight: 700;
    }

</style>